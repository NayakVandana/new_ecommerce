import type { CatalogCategory } from '@/store/catalogTypes';

export const womenStoreCache = {
    ready: false,
    womenGenderId: null as number | null,
    shopCategories: [] as CatalogCategory[],
};
